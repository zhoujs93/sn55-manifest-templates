close:
  strategy: avg
  models:
  - path: close/optuna_k=0_pruned.joblib
min:
  strategy: avg
  models:
  - path: min/model1v2_quantile.joblib
max:
  strategy: avg
  models:
  - path: max/model1v2_quantile.joblib
