close:
  strategy: avg
  models:
  - path: close/optuna_k=0_pruned.joblib
min:
  strategy: avg
  models:
  - path: min/model4_old.joblib
max:
  strategy: avg
  models:
  - path: max/model4_old.joblib
